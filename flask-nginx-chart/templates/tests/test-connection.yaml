apiVersion: v1
kind: Pod
metadata:
  name: "flask-nginx-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ["wget"]
      args:
        - "http://flask-nginx-flask:5000"
  restartPolicy: Never
